# My Individual Project — Routes Plan

Fill this out BEFORE you write code. Adapt to your own assigned domain.

| Method | Route | What it does |
|---|---|---|
| POST | `/api/...` | e.g. submit a new prediction request |
| GET | `/api/...` | e.g. fetch prediction history |
| GET | `/api/.../<id>` | e.g. fetch one record |
| PUT | `/api/...` | e.g. update a record |
| DELETE | `/api/...` | e.g. delete a record |

## Notes
- Which of these need authentication (login_required)?
- Which ones call your ML model?
- What does each response JSON look like?
